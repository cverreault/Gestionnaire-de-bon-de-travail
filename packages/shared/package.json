{
  "name": "@taskmgr/shared",
  "version": "0.1.0",
  "private": true,
  "description": "Code partagé sans UI pour l'app mobile Dispatch2Go : types domaine, contrats API mobile, utilitaires purs, locales technicien. Consommé en source TypeScript (pas de build). Le backend ne l'importe jamais (ADR-014).",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./locales/*": "./locales/*"
  },
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "jest"
  },
  "devDependencies": {
    "@types/jest": "^29.5.14",
    "jest": "^29.7.0",
    "ts-jest": "^29.2.5",
    "typescript": "~6.0.3"
  }
}
